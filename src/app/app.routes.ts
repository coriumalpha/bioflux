import { Routes } from '@angular/router';
import { CloudAtlasComponent } from './features/cloud-atlas/cloud-atlas.component';

export const routes: Routes = [
    { path: '', component: CloudAtlasComponent }
];
